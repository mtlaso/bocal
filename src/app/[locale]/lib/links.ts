/**
 * LINKS defines as the links in the app.
 */
export const LINKS = {
  login: "/login",
  dashboard: "/dashboard",
  archive: "/archive",
  feed: "/feed",
  newsletter: "/d/newsletter",
} as const;
